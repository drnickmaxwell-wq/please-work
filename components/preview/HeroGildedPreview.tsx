"use client";

import ChampagneHero from "@/components/home/ChampagneHero";

export default function HeroGildedPreview() {
  // Reuse the exact homepage hero stack (gradient + waves + motion + grain)
  return <ChampagneHero previewOverride />;
}
